export interface IContact {
  no: number,
  observaciones: string;
  referencia: string;
  contacto: string;
  members: string[];
  userId: string;
  correo: string;
  telefono: string;
  empresa: string;
  acciones: string[]
}
